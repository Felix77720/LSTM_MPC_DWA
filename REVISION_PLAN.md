# 本轮代码与实验修订计划

日期：2026-09-04。分工：Sol 只读检查并提出计划，Luna 实现代码与稿件，主任务复核运行和证据。

## 验收目标

1. MPC 无人机预测状态与障碍预测状态使用同一未来时刻。
2. 单种子入口不覆盖多种子证据；记录实际控制步数、配置、种子与模型标识。
3. 保留 S1–S4；新增固定的重复遭遇场景，障碍轨迹在运行前生成，不依赖实际无人机状态。不人为修改无人机轨迹或增加强制绕路点。
4. 用有符号横向偏移、垂直偏移、同时刻真实净空和无障碍对照检验多次绕行。目标是至少三个分离的实质偏移事件；未达到则如实记为未达到。
5. 增加三方法比较、MPC/DWA 单层预测消融、10/20/30 步注入时域消融和可复现多种子入口。试验逐条落盘，保留失败结果。
6. 新论文稿件从已完成的结果表生成，未运行实验明确列为待验证，不将单种子场景间差异写成重复试验方差。

## 初稿证据缺口

- 旧 `multiseed_comparison.csv` 实际仅有一个种子，不能作为多种子证据。
- 旧结果中匀速外推的总体 ADE/FDE 略低于 LSTM，不能写成 LSTM 对所有基线均有预测优势。
- 旧 Collisions 是障碍物与时间步重叠计数，不是独立碰撞事故次数。
- 路径长度及 RMSE 不随 S1–S4 对所有方法单调增加。
- 闭环预测误差来自不同运行时长，严谨的预测器比较仍需相同离线窗口。
- 现有每步 CPU 耗时大于 0.1 s 采样周期，不能声称满足该周期实时性。
- 合成场景、固定训练模型与少量种子不能证明实机安全或广泛泛化。

## 术语与稿件结构

算法研究，沿用中文 EI 初稿结构与英文摘要，不套用 Nature 投稿格式。
固定术语：MPC（模型预测控制）、DWA（动态窗口法）、LSTM（长短期记忆网络）、CV（匀速外推）、带符号净空、绕行事件、预测注入时域。
论证顺序：方法与时间对齐 → 重复遭遇及无障碍对照 → 方法比较和消融 → 失败、计算代价与泛化边界。

## 清理范围

确认可再生的旧 LaTeX 编译中间文件 `.aux`、`.out`、`.log` 可删除。
模型、源代码、训练入口、历史稿件、结果 MAT/CSV 和实验日志保留；不以文件名陈旧作为删除依据。

## 运行与证据说明

MATLAB 沙箱内启动出现文件系统错误；获批沙箱外检查确认 R2025a、Deep Learning Toolbox 和 CasADi 3.8.0 可用。
运行结果和本轮实际完成范围见后续 `REVISION_STATUS.md`；本文件是计划，不是完成证明。

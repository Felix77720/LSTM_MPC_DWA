# 本轮修订状态

更新时间：2026-09-07。本文档只记录可复现的代码和证据边界，不把未完成试验写成结论。

## 已完成

- 修正 MPC 动力学状态与动态障碍预测的时刻配对：障碍第 `k` 个预测与 `X(:,k+1)` 比较；信任度计算同步使用 `ref(k+1)`。
- 保留三维回归原线路软代价、安全净空筛选、DWA 空候选回退、预测窗口清空后的航向回线阻尼和绕行事件统计。
- `static`、`cv`、`kalman`、`lstm` 四种方法已接入主闭环、固定窗口评估、鲁棒性实验、随机场景扩展和补充实验入口。
- LSTM 训练协议为 800 条独立轨迹、640/160 条轨迹级训练/验证划分、训练集统计量归一化、0.03 m 历史扰动与 1 步延迟增强、Adam、L2、梯度阈值、验证早停和最佳验证损失模型选择。当前模型文件为 `results/lstm_predictor.mat`，SHA-256 为 `61d84f53263b9ed568c15a3a0fa2dc2fb04d7b1b4e4f669ae38effc4e3e3c25d`。
- 固定窗口评估：340 个重叠窗口/方法；static、cv、Kalman、LSTM ADE/FDE 分别为 2.07/3.92、0.51/1.23、0.56/1.31、0.49/1.14 m。窗口重叠，不作独立重复或显著性推断。
- 固定场景三种子闭环：static、cv、Kalman、LSTM 成功数分别为 10/12、9/12、9/12、12/12；逐运行失败类型、碰撞、净空和预测/DWA/MPC/状态指标时间分解保存在 `results/multiseed_comparison.csv`。
- 统一噪声—延迟压力测试：单一 S4 六障碍场景加入 clean、0.05 m 噪声、1 步延迟和联合条件，四方法均使用每次运行固定且共享的观测流。最新目录为 `results/robustness_unified/sensing_20260906_160557`；LSTM 成功数依条件为 2/3、2/3、2/3、1/3。
- 扩展随机套件：场景种子 20260905 生成 12 个随机场景，3 个运行种子、4 种方法共 144 次运行。static、cv、Kalman、LSTM 成功数为 26/36、33/36、33/36、36/36；结果目录为 `results/random_eval/random12_20260906_134750`。
- 论文材料已补充中文/英文标题、摘要、关键词、作者单位、Kalman 基线、训练验证协议、逐运行失败与时间分解、参考文献扩展和通用 EI A4 双栏排版稿。
- 最终 DOCX 已经 Word→PDF→PNG 逐页渲染检查；表格均从新页开始，预测图缩放后图注未跨页，正文/页眉页脚/表格字体为黑色。

## 当前闭环验证边界

LSTM 在固定场景中并非无条件成功：压力套件的联合噪声—延迟条件仅成功 1/3，且 cv/Kalman 在该条件下成功 3/3。压力套件只覆盖一个 S4 场景，随机套件是固定生成器下的仿真证据，均不能替代真实传感器、漏检、障碍物交互或硬件在环验证。

固定主实验平均每步计算时间约为 95–99 ms，压力条件约为 126–206 ms；当前结果不支持稳定 10 Hz 实时部署的声明。Kalman 的预测时间还显示出实现层面的额外开销。

参考文献 [1] 的题名、作者、期刊和网络首发日期已按本地原始稿和网络首发链接核对，但正式卷期、页码和 DOI 尚未取得可访问的权威记录，见 `paper/REFERENCE_CHECK.md`。具体会议名称和官方模板也未提供，因此当前版式是通用 EI 双栏 A4 profile，收到模板后仍需做最终模板匹配。

## 可复现实验入口

```matlab
cd('C:/Users/Felix/Desktop/project/LSTM_MPC_DWA')
retrain_lstm
run_multiseed
evaluate_prediction_fixed
run_robustness_experiments
run_random_multiseed
```

固定窗口、闭环、压力和随机场景输出均按运行目录保存；每次闭环记录 `FailureType`、`FallbackSteps`、步骤 P95，以及预测、DWA、MPC 和状态/指标计算时间。
